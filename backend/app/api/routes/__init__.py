# backend/app/api/routes/__init__.py
#
# Marks the `routes` directory as a Python package.
# Routers are imported by api/router.py — not here — to keep the import
# graph explicit and avoid circular import risks.
